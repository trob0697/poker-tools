import { ReactElement } from "react";

function EquityCalculator() : ReactElement {
    return (
        <div style={{color: "white"}}>Equity Calculator Page</div>
    );
}

export default EquityCalculator;
